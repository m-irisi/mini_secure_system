## App taks:
- makes own API 

### How to run:
In terminal paste:
``` uvicorn backend.main:app --reload```

### Frontend:
- sends JSON requests and displays results

### Backend:
- validates input
- manages transactional data
- solves a small graph optimization problem
